{
  "name": "hiredis",
  "scripts": {
    "test": "exit 0",
    "deploy": "haikro build deploy --app hiredis --heroku-token ${HEROKU_AUTH_TOKEN} --verbose"
  },
  "dependencies": {
    "express": "^4.10.7",
    "haikro": "^1.8.1",
    "next-build-tools": "^1.2.0",
    "then-redis": "^1.2.2"
  }
}
