{
  "name": "hiredis",
   "scripts": {
     "test": "exit 0",
     "deploy": "npm prune --production && haikro build deploy --app hiredis --heroku-token ${HEROKU_AUTH_TOKEN} --verbose"
   },
  "dependencies": {
    "body-parser": "^1.9.0",
    "cors": "^2.5.2",
    "es6-promise": "^1.0.0",
    "express": "^4.8.x",
    "forever": "^0.13.0",
    "lodash": "^2.4.1",
    "next-build-tools": "^1.3.1",
    "next-metrics": "https://github.com/Financial-Times/next-metrics/archive/v1.0.2.tar.gz",
    "then-redis": "^0.3.12"
  },
  "engines": {
    "node": "0.10.x"
  }
}
